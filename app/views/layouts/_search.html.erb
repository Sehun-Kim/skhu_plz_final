<%= form_tag lectures_path, :method => :get do %>

        <%= text_field_tag :search, params[:search], placeholder: "search" %>

        <%= button_tag fa_icon('search'), class: "button secondary postfix" %>

<% end %>